# Copy this file to .env and fill in your values
# Never commit .env to source control

# SQL Server connection (AdventureWorks on Docker)
DB_SERVER=localhost
DB_PORT=1433
DB_USER=sa
DB_PASSWORD=YourPass123!
DB_DATABASE=AdventureWorks

# MCP server
PORT=3000

# Optional: shared secret for Bearer token auth
# MCP_SECRET=change-me-before-production
